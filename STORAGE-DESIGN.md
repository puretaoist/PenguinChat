# 存储架构设计：为什么不学 QQ，而学 Telegram

> 目标：**装完几百 MB，用一年还是几百 MB。**
>
> 这条约束不是「优化项」，而是架构决策。一旦按官方那种「每个业务自己建目录、
> 自己管缓存」的方式起步，后期再想收口就要动所有业务代码。

---

## 一、先看清官方 QQ 的存储病灶（有证据）

对目标 APK（9.3.60）全 dex 扫描，`Tencent/MobileQQ/` 下的目录分布：

| 现象 | 扫描到的证据 | 后果 |
|---|---|---|
| **业务目录爆炸** | 40+ 个顶级目录：`apollo`、`video_story`、`shortvideo`、`pubaccount`、`qzone`、`readinjoy`、`chatpic`、`head`、`diskcache`… | 每个目录各有一套生命周期，无人统管 |
| **按业务分配缓存配额** | 配置键 `businessCacheSizeWeight`、`commonCacheMaxSizeRatio`、`imageCacheRatioBelowAndroid8` | 各业务申报权重，总和无人保证；新增业务就加配额，旧配额不回收 |
| **同一张图多处落盘** | `chatpic/`（聊天显示）+ `.thumbnails/`（缩略图）+ `head/`（头像） | 一张图最多 3 份 |
| **表情多处落盘** | `QQfile_recv/emoj`、`QQfile_recv/.tmpsysemotion`，另有账号级 `CustomEmotionData` | 一个表情多份 |
| **缓存按账号隔离** | `Tencent/MobileQQ/.apollo/image_cache/599596562`（该数字为 UIN） | **N 个账号 = N 份缓存** |
| **接收目录另有独立结构** | `QQfile_recv/{audio,emoj,pic,short_video,.thumbnails,.tmp}` | 与业务目录并存，同一内容两套 |

**最致命的是最后两条**：账号隔离 + 多处落盘。它们让存储占用随
「账号数 × 业务数 × 内容数」增长，而不是随「内容数」增长。

官方并非没有缓存管理——它有一整套配置项。但管理粒度是「每个业务自己管」，
所以**总量不可证明有界**。用户看到的就是「越用越大，最后几十 G」。

---

## 二、Telegram 为什么能保持几百 MB

抽象出来是四条：

1. **内容寻址**：文件按哈希定位，同一内容全局只有一份
2. **单一缓存池**：所有媒体共用一个预算，LRU 竞争，不按业务切分
3. **缓存可丢弃**：缓存里的东西随时可从服务器重新拉取，因此可以放心清
4. **流式消费**：视频/音频边下边播，不必整段落盘

关键在第 3 条：**只要「能重新下载」，缓存就永远是可牺牲的**，
于是可以用一个硬上限粗暴地卡住它。官方 QQ 的困境在于很多本地文件
既是缓存又是用户数据，边界不清，于是既不敢删又管不住。

---

## 三、我们的设计

### 3.1 三大构件

```
lib/infra/storage/
├── blob_store.dart       内容寻址存储（CAS）
├── cache_policy.dart     全局预算 + LRU 淘汰
└── storage_manager.dart  两级语义 + 引用清单
```

磁盘布局：

```
<app 私有目录>/
  data/
    blobs/ab/abcdef0123…    ← 实际数据，文件名即 SHA-256
    tmp/                    ← 写入中转（先写 tmp 再 rename，保证原子）
  library.json              ← 引用清单：可读名 -> 内容标识（每条约 80 字节）
```

### 3.2 关键机制

| 机制 | 做法 | 解决的官方问题 |
|---|---|---|
| **内容寻址** | 路径 = `blobs/<hash前2位>/<完整hash>` | 同一内容天然去重；不按业务建目录 |
| **两级分桶** | 前 2 位十六进制作子目录 | 避免单目录堆几十万文件导致查找退化 |
| **单一预算** | 一个数字卡住整个缓存区 | 取代 `businessCacheSizeWeight` 的分散配额 |
| **两级语义** | 缓存（可回收）/ 库（引用清单，不复制数据） | 「保存」不再产生副本 |
| **大小闸门** | 超过 `maxCacheableBytes` 的内容不落盘 | 堵住视频整段落盘导致暴涨 |
| **账号无关** | 路径与清单都不含 UIN | 多账号共享缓存，切号不重复下载 |
| **原子写入** | 先写 `tmp/`，再 `rename` | 进程被杀不留半截文件 |

### 3.3 为什么「保存」不复制数据

这是最能直接省出空间的一条。

曾考虑用硬链接 + inode 引用计数。放弃原因：

- Dart 的 `FileStat` 不暴露 inode（`dart:io` 未提供该字段）
- 即便调平台接口拿到，跨文件系统硬链接会失败
- Android scoped storage 下行为不一致

改用**显式引用清单**：`library.json` 里存 `可读名 -> 内容标识`。
「保存一张图」只是加一条映射，数据仍只有 `data/` 里的那一份。

只有用户**显式导出到相册/下载目录**时才真正复制——那是用户的主动意图，
不属于「客户端重复存储」。

代价：库里的文件不是磁盘上可直接浏览的实体文件。
对本应用无影响（界面从 CAS 读取）；需要给外部应用看时走 `exportTo`。

### 3.4 淘汰与保护的边界

```
淘汰候选 = 全部内容 − 被 library.json 引用的内容
```

因此：

- **清理缓存**（设置页按钮）→ 只清未引用内容，用户收藏不受影响
- **自动淘汰**（超预算触发）→ 同上，被引用的跳过
- **解除引用**（用户删除收藏）→ 内容重新变成候选，后续淘汰自然回收

这条边界必须有测试兜住，否则「清缓存把用户收藏清了」是灾难级 bug。
已覆盖：`tool/storage_selftest.dart` 中 5 个相关用例。

---

## 四、预期占用对比

假设用户重度使用一年，收到 5 万张图、2000 个视频、300 个表情包：

| 项 | 官方 QQ | 本设计 |
|---|---|---|
| 图片（去重后约 800MB 原始） | 缓存 2–3 份 + 多业务目录 → **2–4GB** | **≤ 512MB**（受预算硬卡） |
| 视频 | 整段落盘 + `shortvideo`/`video_story` 两份 → **数 GB** | **0**（超阈值只流式播放） |
| 表情包 | 按账号 × 多目录 → **随账号数增长** | **单份，与账号无关** |
| 收藏的图 | 另存一份 → **+N** | **0**（引用清单） |
| **合计** | **10GB+** | **≤ 512MB + 清单（<1MB）** |

「≤ 512MB」不是估计值，而是**代码层面的硬约束**：
`StorageBudget.cacheBytes` 是唯一的容量入口，超出必定触发淘汰。

---

## 五、APK 体积同理

占用不只看运行时，安装包本身也是。目标 APK 是 391MB，构成：

| 项 | 官方 QQ | 我们的做法 | 预期 |
|---|---|---|---|
| dex | 41 个，约 400MB 字节码 | 只实现用到的协议子集 | < 5MB |
| native `.so` | 203 个（含游戏引擎、AI 模型、虚拟形象） | Flutter 引擎自带 | ~8MB/ABI |
| 内置表情/主题资源 | 大量随包发布 | **按需下载，不入包** | ~0 |
| 多 ABI | 打包多套 | `--split-per-abi` 只出 arm64 | 减半 |
| Dart 代码 | — | `--release` 树摇 + 混淆 | 小 |

预期 release APK 约 **15–25MB**（对比官方 391MB）。
其中关键的一条是：**不随包发布可下载资源**。官方把大量表情、皮肤、
动效打进 APK，我们的做法是全部按需拉取并进 CAS——这也顺带让缓存策略统一。

---

## 六、验证

```bash
dart run tool/storage_selftest.dart
```

覆盖 48 个断言，分组：

| 分组 | 关键断言 |
|---|---|
| CAS 去重 | 相同内容同一标识；磁盘只有一份；范围读取 |
| 预算与淘汰 | 高/低水位计算；LRU 顺序；受保护项不参与 |
| 两级语义 | **保存后总字节数不变**；清缓存跳过收藏；解除引用后可回收 |
| 多账号 | 同一图片两次入库只有一份 |
| 大文件 | 超阈值返回 null 且确实未落盘 |
| 统计 | 缓存/库划分正确，不重复计入 |
| 持久化 | 重启后清单与内容都在；无悬空引用 |
| 导出 | 显式导出才复制，内部仍一份 |

---

## 七、已知边界与后续

1. **缩略图尚未接入**。设计上应生成一次、存入 CAS、由 `blob id` 引用，
   避免官方 `.thumbnails/` 那种「原图 + 缩略图各一份」。
   但缩略图与原图是不同内容（不同哈希），所以「一份」指的是
   「同一尺寸只生成一次」，不是与原图合并。
2. **`lastAccess` 依赖文件系统 atime**，在 `noatime` 挂载下不可靠。
   正式实现应在读取时把访问时间写进一个轻量访问索引，
   而不是信任 `FileStat.accessed`。
3. **未接入真实媒体流**。`readRange` 已就绪，但需要配合服务端支持
   HTTP Range 请求，以及播放器的分段回调。
4. **未做并发压测**。多个下载任务同时写同一内容时的竞争已处理
   （`put` 里的 rename 冲突分支），但未做压力验证。
