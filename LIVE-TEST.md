# 真机测试说明

> 这份文档回答一个问题：**现在能不能拿真实账号去连腾讯服务器，怎么连，怎么判读结果。**
>
> 结论先说：**可以，但要分两步走，且第二步只做一次。**

---

## 一、现在的验证状态（说实话）

| 环节 | 验证方式 | 状态 |
|---|---|---|
| TEA 加解密 | oicq 黄金向量 + 官方反编译对照 | ✅ 已验证 |
| TLV 打包（48 条） | oicq 原始模块生成的黄金向量 | ✅ 已验证 |
| OICQ / 登录信封 | oicq 黄金向量 | ✅ 已验证 |
| 传输层分帧 | 真实本地 TCP mock 服务器 | ✅ 已验证 |
| 请求 body 头格式 | **官方反编译代码**（`oicq_request.java:362`） | ✅ 已验证 |
| TLV 准入条件（guard） | 官方 `k.java` / `j.java` 逐 case 核对 | ✅ 已验证 |
| **响应布局** | 往返构造（自造响应→自解） | ⚠️ **非黄金向量** |
| **服务端是否接受** | —— | ❓ **只能真机验** |
| 8.9.50 的 `apkName` | 按 oicq 约定推导 | ⚠️ 未核实 |

**响应解析是最大的未知。** 设备上的流量在 TLS/ECC 之下，抓不到明文响应样本，
所以它目前只经过往返测试——能抓偏移写错、读写不对称、密钥用错，
**抓不到"官方真实布局与我的推断不符"**。

---

## 二、第一步：dry-run（离线，零风险）

```bash
dart run tool/qq8_live_smoke.dart
```

不联网、不需要账号。它会把登录包完整组装出来并打印：

- 客户端档案（TLV 表长度、ssoVer、subAppId）
- 登录 body 的**子命令、TLV 个数、每个 TLV 的编号与长度**
- 三层信封尺寸，并校验首 4 字节声明的长度与实体一致
- 包头前 48 字节的十六进制

**当前输出（8.9.50 档案）：**

```
子命令 = 9 (密码登录)
TLV 个数 = 26
body 总长 = 1097
  0x0018  22   0x0001  20   0x0106 120   0x0116  14   0x0100  22
  0x0107   6   0x0108  29   0x0142  24   0x0144 328   0x0145  16
  0x0147  29   0x0154   4   0x0141  20   0x0008   8   0x0511 211
  0x0187  16   0x0188  16   0x0194  16   0x0191   1   0x0202  32
  0x0177  17   0x0516   4   0x0521   6   0x0525   8   0x0544   0
  0x0545   0
（顺序表 37 项，被 guard 滤掉 11 项）

body 1097 → OICQ 信封 1230 → 登录信封 1383
首 4 字节声明的总长 = 1383  ✓
```

**这一步要人工核对的地方：**

1. **26 个 TLV 是否合理**。参照系：参考实现 oicq 的密码登录只发 **24 个**，
   且长期实测可用。我们是它的 24 个 + `0x544` + `0x545`——多出来的两个
   正是设备证明块，而它们在本实现里都是**官方降级形态**。
2. **`0x0544` 和 `0x0545` 长度为 0**。这是刻意的：8.9.50 的 `0x544` 在安全
   SDK 不可用时就是空 body（`liteSign` 初值 `new byte[0]`），`0x545` 拿不到
   QIMEI 时空 body 也是官方行为。**它们是"存在但为空"，不是被滤掉。**
3. **被滤掉的 11 项**：`0x104`（首登无缓存盐）、`0x112`（uin 登录不发）、
   `0x166`、`0x172`、`0x185`、`0x16A`、`0x201`、`0x318`（二维码专用）、
   `0x400`（首登无票据）、`0x529`（三版本都无构建点）、`0x548`。

---

## 三、第二步：真实发送（有风险，只做一次）

### 3.1 准备

```powershell
$env:QQ_LIVE_UIN="<小号 QQ 号>"
$env:QQ_LIVE_PWD="<小号密码>"
$env:QQ_LIVE_CONFIRM="I_UNDERSTAND_THE_RISK"
dart run tool/qq8_live_smoke.dart --send --export-log
```

**用小号。** 别用常用号。

### 3.2 两道闸门（不是仪式）

1. 命令行必须显式给 `--send`；
2. 必须显式设置 `QQ_LIVE_CONFIRM=I_UNDERSTAND_THE_RISK`。

本工程的安全层（`lib/kernel/safety/`）就是为避免"顺手连生产环境"而写的，
工具层遵守同一条纪律。没给确认串时工具会直接退出，**不联网**。

### 3.3 限流：只发一次，不重试

用 `LoginAttemptLimiter`（持久化到磁盘，重启不清零）：

| 参数 | 值 |
|---|---|
| 窗口 | 10 分钟 |
| 窗口内上限 | 3 次 |
| 基础冷却 | 30 秒，随连续失败递增 |
| 最大冷却 | 8 分钟 |
| 硬锁 | 连续失败 5 次 → 锁 24 小时 |

**为什么要持久化**：如果重启应用就能清零，限制形同虚设，
而"重启后继续猛试"正是最容易被风控捕捉的行为模式。

**失败就停。** 重试本身是风控特征——这比"多发几次碰运气"安全得多。

### 3.4 凭据不进日志

口令、`tgtgt`、票据、`d2key`、会话密钥一律不写日志。
`Redact` 里列了 **29 个凭据类键名**，命中就打码；确实需要记值的场合
只输出**指纹**（长度 + 前 4 字节），够比对"两次是不是同一个"，不泄漏内容。

响应只打印 `type` 和 TLV 的**编号与长度**，不打印票据内容。

---

## 四、怎么判读响应

```
type = 0    → 成功。会再解 0x119 票据块，打印各票据的**长度**
type = 2    → 需要滑动验证码，会打印 0x192 里的验证地址
type = 204  → 设备锁 / 二次验证
其它        → 失败（40 常见于密码错误）
```

**type=0 且票据长度符合预期**，说明整条链路（组包 → 传输 → 解密 → 解析）都对了。

**type 是预期值但票据解不出**，说明 0x119 那层（用 tgtgt 解密）有问题。

**完全解析失败 / 长度声明越界**，说明响应布局与推断不符——这正是要暴露的东西。

---

## 五、失败之后怎么办

1. **停下来**，不要重试。
2. `--export-log` 导出的报告已经含全部上下文，把它留存。
3. 如果拿到的是一段原始响应字节，**把它补成黄金向量**——
   这是把响应解析从"往返测试"提升到"已验证"的唯一正路。
4. 继续投入前先评估：即使全部跑通，**本实现的天花板就是 oicq 那一档的
   信任级别**（`0x544` / `0x553` / `0x545` 三个设备证明块都只能发官方降级形态）。
   这个结论在真机测试里不会改变。

---

## 六、一条更安全的前置路线（推荐先走）

在动我们自己的实现之前，可以先用**参考实现**（oicq）做一次真实登录，
把它收到的**原始响应字节**导出来，喂给我们的解析器：

1. oicq 是已知可用的实现，"能不能登"这一问在它身上没有不确定性；
2. 拿到真实响应后，我们就有了响应解析的**黄金向量**；
3. 之后再跑自己的实现时，**只剩"我们的组包对不对"一个变量**。

这样把一次有风险的操作，拆成"验证解析器"和"验证组包"两步，
每一步的失败都指向明确的原因，而不是一堆可能性混在一起。

---

## 七、和日用线的关系

真机测试属于**协议研究线**（QQ 8.2.11 / 8.9.50 自实现协议），
它的定位是课程作业与协议学习。

**日用**那条线走的是 OneBot + NapCat（跑官方客户端本体），
账号风险最低，与本文档无关。
