# 账号风险说明与防护设计

> **请先读这一节。**
>
> 本文档不是免责声明，而是对你真实处境的说明。
> 结论是：**连真实腾讯服务器做实验，风险无法通过工程手段消除。**
> 本文档说明哪些风险能降低、哪些不能、以及我做了什么。

---

## 一、不可消除的风险

以下三条是客观事实，任何代码都改变不了：

### 1. 违反《QQ 用户协议》

腾讯明确禁止使用非官方客户端接入。这不是「灰色地带」，是明确的违约行为。

### 2. 账号可能被永久封禁

腾讯有权对违约账号采取限制措施，且**申诉不保证成功**。
永久封禁是不可逆的——账号里的联系人、群、聊天记录、绑定的服务都可能一起损失。

### 3. 客户端行为特征与官方不同，会被识别

腾讯的风控不只看协议对不对，还看行为像不像真人用官方客户端：

| 维度 | 官方客户端 | 我们的客户端 |
|---|---|---|
| 设备指纹 | 完整 Qimei | **不采集**（我们明确不伪造） |
| 行为轨迹 | 大量前置埋点 | 几乎没有 |
| 请求节奏 | 人类操作节奏 | 程序化节奏 |
| 运行环境 | 完整 QQNT 框架 | 精简实现 |

**我们明确不补齐这些差距**——补的过程就是伪造，属于规避安全措施。
所以这个客户端在风控视角下**天然是可疑的**。这是设计取舍，不是缺陷。

---

## 二、反编译得到的风控证据

对目标 APK（9.3.60）的扫描结果：

### 2.1 登录失败会被连设备指纹上报

```
com/tencent/mobileqq/login/ntlogin/LoginFailedQimeiReporter
com/tencent/mobileqq/reportlog/api/impl/QimeiReportLoginFailServlet
com/tencent/mobileqq/reportlog/api/impl/QimeiReportLoginFailFileManager
```

这是一条**专用链路**：每次登录失败 → 打包失败事件 + 设备指纹 → 上报。
`...FileManager` 的存在说明失败记录还会**先落盘再补报**——网络不通也逃不掉。

**含义**：`失败次数` 是一个被明确采集的信号。这是本设计要防的头号风险。

### 2.2 设备指纹是跨业务共享的

Qimei 出现在 **24 / 41 个 dex** 文件中，SDK 位于 `com.tencent.qimei.sdk.QimeiSDK`。
说明它是全公司共享的设备标识，不只是登录模块在用。

`oicq.wlogin_sdk.listener.QimeiListener` 是登录 SDK 读取它的接口。

### 2.3 协议层就有风控与设备锁的位置

| 证据 | 含义 |
|---|---|
| `tlv_t402` / `tlv_t403` | 风控信息 / 风控结果 TLV |
| `oicq.wlogin_sdk.devicelock.*`（13 个类） | 完整的设备锁机制（`DevlockInfo`、`TLV_QuerySig`、`TLV_SppKey`） |
| `EcdhService.SsoNTLoginAuthNewDevice` | 新设备鉴权 |
| `EcdhService.SsoNTLoginCheckGateWayCode` | 网关验证码 |
| `oicq.wlogin_sdk.listener.ReportListener` | 专用上报接口 |

**这些机制我们一个都不绕过。** 它们出现时，正确反应是停止并告知你。

---

## 三、我们做了什么

不是「消除风险」，而是**让无心之失不容易发生**。

### 3.1 默认离线（最重要的一条）

```dart
ConnectionMode _mode = ConnectionMode.offline;   // 默认值
```

三种模式：

| 模式 | 行为 | 风险 |
|---|---|---|
| `offline` | 不联网，UI 用内置样例数据 | 无 |
| `loopback` | 内存回环，协议链路完整可跑 | 无 |
| `realServer` | 连腾讯生产服务器 | **有** |

**默认是 `offline`。** 这一条能挡掉绝大部分「我以为在测 demo，其实在操作真账号」的事故。
前两层足以完成 M1–M4 的绝大部分开发与演示。

### 3.2 开启真实服务器需逐条确认

不能一键同意。必须复述全部 4 条风险要点，且代码用**关键词匹配**检查
是否真的复述了（防止随手点过）：

```dart
const requiredKeywords = [
  ['封禁', '限制登录'],
  ['设备指纹', 'Qimei', 'qimei'],
  ['伪造', '绕过'],
  ['测试账号', '专门注册'],
];
```

同意记录**带声明版本号**。声明更新后旧同意自动失效，必须重新确认。

### 3.3 登录尝试硬限制（针对头号风险）

既然失败会被上报，就不能让失败次数失控：

| 约束 | 默认值 | 理由 |
|---|---|---|
| 窗口内次数 | 10 分钟 3 次 | 对齐人类操作的合理频率 |
| 递增冷却 | 30s → 1min → 2min → … → 8min | 指数退避，不是固定间隔 |
| 连续失败硬锁 | 5 次后锁 24 小时 | 阻断「一直试」的行为模式 |
| **持久化** | 写入磁盘 | **重启应用不清零** |

持久化这一条是关键的：如果重启就能重置计数，那限制形同虚设，
而「重启后继续猛试」恰恰是最容易被风控捕捉的模式。

### 3.4 禁止自动重试

```dart
static bool mustAbort(RiskSignal s) => s != RiskSignal.none;
```

**除「无信号」外，任何服务端拒绝都要求立即中止。** 代码里不存在自动重试路径。

这与普通应用的体验习惯相反——但在这里，「失败自动重试」等于主动刷高失败计数。

### 3.5 风控信号识别与处置建议

识别到的信号与对应建议（措辞刻意不诱导继续尝试）：

| 信号 | 建议 |
|---|---|
| 需要短信验证 | 重要账号请停止，改用官方客户端完成验证 |
| 需要图形验证码 | **通常意味着频率已被判定异常**，强烈建议立即停止，等待数小时 |
| 新设备鉴权 | 正常保护机制，但请勿反复触发 |
| 账号被限制 | 立即停止所有自动尝试，用官方客户端确认状态，走官方申诉 |
| 设备被锁定 | 需通过 QQ 安全中心解锁，本客户端无法也不应绕过 |

### 3.6 随时可切断

`killSwitch()` 一键回到离线模式并持久化。同意也可随时撤销。

---

## 四、我们明确不做的事

以下都是「能让实验更顺利」的手段，但全部属于**规避安全措施**，不做：

| 不做 | 原因 |
|---|---|
| 伪造 Qimei 设备指纹 | 规避设备识别 |
| 绕过图形验证码 | 规避风控挑战 |
| 绕过设备锁 | 规避账号保护机制 |
| 伪装请求特征以冒充官方客户端 | 规避行为识别 |
| 共享/上传他人的设备指纹或票据 | 可能涉及他人账号安全 |

这些做法短期可能「跑得通」，但：一是性质变了（从逆向学习变成规避安全措施），
二是**会显著提高账号被永久封禁的概率**——你越像在伪装，风控越会重点照顾。

---

## 五、建议的操作方式

按风险从低到高：

### ✅ 推荐：不连真实服务器

用 `offline` / `loopback` 模式完成开发与演示。
协议实现的正确性可以通过单元测试、自检脚本、抓包回放来验证，
**不需要真实账号**。

课程作业考察的是「你理解并实现了协议」，不是「你成功登录了几个账号」。

### ⚠️ 如必须连真实服务器

1. **注册一个专门的新 QQ 号**，绝不使用主账号
   - 主账号往往绑定了支付、群管理、重要联系人、其它服务
2. 先和老师确认这是作业允许的范围
3. 做好该账号可能损失的心理准备
4. 一次成功后**停止反复调试**——每次失败都在累积标记
5. 遇到任何验证码/短信/设备锁要求，**立即停止**，不要反复尝试
6. 优先用官方客户端确认账号状态，不要用我们的客户端去「试探」

### ❌ 不要做

- 不要用主账号
- 不要在失败后反复重试
- 不要尝试关闭本文档里的限制
- 不要把账号密码写进代码、配置文件或提交记录

---

## 六、如果账号真的出问题了

1. **立即停止**用本客户端的一切操作（继续操作只会加重标记）
2. 用**官方客户端或官网**确认账号状态
3. 若被限制，走官方申诉流程（腾讯客服 / QQ 安全中心）
4. **不要**隐瞒使用第三方客户端的事实去申诉——一旦被发现，申诉会更难
5. 本次经验正好可以写进报告：「我理解了风控机制并付出了代价」比
   「我绕过了风控」是更好的课程结论

---

## 七、验证

```bash
dart run tool/safety_selftest.dart
```

**53 个断言全部通过**，关键覆盖：

| 分组 | 关键断言 |
|---|---|
| 尝试限制 | 窗口上限、指数退避、连续失败硬锁 |
| **持久化** | **重启后计数保留、重启后仍受冷却** |
| 默认模式 | 默认离线、默认无同意 |
| 知情同意 | 条目不足拒绝、敷衍确认拒绝、完整确认通过 |
| 跨重启 | 同意持久化、模式持久化 |
| 紧急切断 | 一键回离线、撤销同意 |
| 风控判定 | 各返回码映射、**任何拒绝都中止**、建议不含诱导 |

---

## 八、最后一句

这个项目的技术目标是「理解 QQ 的协议与风控机制」。
**理解了机制之后尊重它，比绕过它更有价值。**

如果你只是想看到「能连上」，那用一个测试号也够了；
如果你是想证明「我懂了」，那 `loopback` 模式加上完整的协议实现与测试，
在评分上不会比真连一次差——而且不会让你损失一个账号。
